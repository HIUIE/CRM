# Changelog

## [2026-04-26]

### 第五十九轮：截图拼接撕裂修复与原生 PDF 导出 (Print/Screenshot Fix & PDF Export)

**问题根因**: 引入 `position: sticky` 吸顶效果后，Chrome/Edge 的"捕获全尺寸屏幕截图"功能在滚动拼接时，sticky Header 干扰了视口高度计算，导致画面出现横向断层与拼接撕裂。

**修复清单**:
- **`@media print` 全局修复**: 在 `src/index.css` 中注入打印媒体查询。当浏览器进入打印流程时，自动将 `position: sticky` 降级为 `position: relative`，隐藏毛玻璃悬浮导航与滚动进度条，解除所有 `overflow` 限制，确保内容完整展开为连续文档流。
- **`.screenshot-safe` JS 注入类**: 针对不触发 `@media print` 的原生截图工具（如 Edge 捕获全尺寸），提供等效的 JS 注入机制。
- **打印事件自动触发**: 新增 `beforeprint` / `afterprint` 全局监听，打印对话框打开时自动注入安全模式，关闭后恢复。
- **手动截图快捷键**: 新增 `Ctrl+Shift+S` 快捷键，可随时手动切换截图安全模式，屏幕右上角弹出 Toast 提示当前状态。

**原生 PDF 导出落地**:
- **新依赖**: 引入 `html2canvas` (DOM→Canvas 渲染) 与 `jspdf` (PDF 生成)。
- **新增 `src/lib/pdfExport.ts`**: 封装 `exportElementToPdf()` 核心函数，使用 2x 高清 html2canvas 将订单内容渲染为 Canvas，自动按 A4 竖版多页切片，逐页注入 PDF，导出前自动注入 `.screenshot-safe` 解除 sticky。
- **按钮升级**: 订单详情页"导出 PDF 报告"按钮由 `window.print()` 替换为 `handleExportPdf`，点击即触发下载。

### 第五十八轮：订单详情页"长文档"重构与悬浮楼层导航 (Long-Scroll Refactoring)

**核心改动**:
- **长文档布局**: 在 `MainLayout` 中识别订单详情路由 `/orders/:orderNo`，将 `h-screen overflow-hidden` 切换为 `min-h-screen` 自然滚动，侧边栏改为 `sticky top-2 self-start`。
- **吸顶操作栏**: 订单详情 Header 改为 `position: sticky; top: 0; z-index: 40`，配合滚动进度条，业务员无需回到顶部即可操作。
- **毛玻璃悬浮楼层导航**: 新增 `.glass-nav` CSS 样式（`backdrop-filter: blur(24px)` + 半透明背景 + 辉光阴影），在详情页右侧固定渲染楼层导航圆点。基于 `IntersectionObserver` 实时追踪当前视口内的业务模块，自动高亮对应导航圆点并以绿色指示条标定位置。
- **AI 诊断快捷入口**: 悬浮导航底部嵌入 AI 诊断按钮，取消原右侧静态面板中的冗余导航区。
- **快捷备注移入主内容流**: 订单快捷备注从右侧面板迁移至主内容区"关联协同任务"下方，统一信息流。

### 第五十七轮：项目基础设施与 AI 协同配置

- **CLAUDE.md**: 创建项目级 AI 上下文索引，含技术栈、路由、认证、API 封装等关键路径说明，以及会话启动时的自检清单与持续记录规则。
- **`.gitignore` 更新**: 新增 `.learnings/` 目录忽略规则，防止 AI 经验记录被提交到版本库。
- **self-improving-agent 技能**: 安装并配置 Cowork 自进化代理技能，实现跨会话的错误记录、经验沉淀与功能需求追踪。

### 第五十六轮：紧急修复 — 文件损坏导致的全面白屏 (Critical Hotfix)

**根因分析 (Root Cause)**: 在代码编辑过程中，`src/pages/OrderDetail.tsx` 被意外拆分为 `.part1/part2/part3` 分片文件，原始文件被覆盖为不完整版本。组件函数在 643 行提前闭合，导致约 400 行 Drawer 表单代码（订单编辑、财务录入、生产排产、装箱管理、物流报关等）全部脱离函数作用域成为孤儿代码。同时 `src/features/order-detail/types.ts` 混入了残留语法垃圾，造成 TypeScript 解析失败。

**修复清单**:
- **`src/pages/OrderDetail.tsx` (Critical)**: 从 git HEAD 恢复完整文件（由 741 行→1141 行），重建所有 Drawer 表单的闭合结构
- **`src/features/order-detail/types.ts`**: 删除第 315-317 行的残留垃圾代码 `}>;` / `summary: string;` / `};`
- **`src/features/order-detail/components.tsx`**: 补全缺失的 `ChevronRight` 图标导入
- **`src/features/order-detail/types.ts`**: 为 `AttachmentMeta` 类型补充 `createdAt` 和 `remark` 字段
- **`server/domain.ts`**: `ATTACHMENT_ENTITY_TYPES` 添加 `'production_photo'` 枚举值，消除类型错误

## [2026-04-25]

### 第五十五轮：全维度隐私加固与商业合规升级 (Privacy & Compliance)
- **AI 脱敏网关 (Sanitization Gateway)**: 引入后端 `sanitizeForAI` 核心引擎，自动识别并抹除 PII（个人身份信息）。所有发往 Gemini/DeepSeek 的业务数据现在均已实现“手术级”脱敏，仅保留业务逻辑，彻底隔离客户联系方式。
- **审计日志深度脱敏**: 重构审计记录逻辑，所有数据的变更快照（Old/New Value）在存入数据库前均经过脱敏处理，杜绝了通过审计后台“倒查”隐私信息的漏洞。
- **前端隐私盾 (Privacy Shield UI)**:
  - **默认遮挡机制**: 客户画像及联系人矩阵中的邮箱、电话现在默认显示为 `sa***@domain.com` 格式。
  - **交互式解锁**: 在关键信息旁增加了“小眼睛”图标，支持点击后二次验证显隐，兼顾了日常操作与资产保护。
- **系统健壮性加固**: 修复了由于组件重构导致的 `ContactCreateDrawer` 路径引用错误，并清理了列表页残留的语法垃圾。

### 第五十四轮：Tooltip 层级穿透与边缘避让修复 (Z-Index & Boundary Detection)
- **Portal 逃逸技术**: 将全局 Tooltip 渲染出口通过 `createPortal` 挂载至 `<body>` 根节点，彻底解决在订单操作栏中被 Header 裁剪、吞噬的问题。
- **碰撞检测 (Smart Flipping)**: 引入视口边界检测算法，当 Tooltip 靠近屏幕顶部或边缘时自动翻转位置，确保提示信息永远在视口可见区域内。
- **层级大一统**: 锁定 Tooltip 的 `z-index` 变量为 `9999`，凌驾于所有 Modal、Drawer 及 Sticky Header 之上。

### 第五十三轮：全局“国家/地区”渲染逻辑大一统 (Country Standardization)
- **封装 `<CountryDisplay />`**: 打造全站唯一的国家渲染组件，支持受控渲染 `[国旗 Emoji] 中文名 (英文名)`。
- **修复详情页视觉 Bug**: 彻底排查并移除了客户 360 画像中破损的图片占位符，修复了国旗加载失败的问题。
- **多端对齐**: 实现客户列表、伙伴列表、360 详情页三方渲染逻辑的 100% 同步。

### 第五十二轮：国家/地区组件重构与双语智能搜索 (Bilingual Country Selector)
- **字典级重构**: 废弃了手动输入的 Input 框，引入标准 ISO 3166 国家字典。
- **中英双语过滤**: 升级过滤引擎，支持“中文名”、“英文名”、“代码”及“拼音首字母（如 zg 匹配中国）”的实时模糊搜索。
- **校验 UI 升级**: 废弃浏览器原生的黑色 Tooltip 提示，接管表单校验机制，实现了带红色预警文字和动态边框反馈的高级表单 UI。

### 客户端热更新机制与安全性加固
- **版本指纹检测**: 在服务端注入 `SERVER_START_TIME` 启动指纹。
- **静默轮询更新 (Version Guard)**: 前端挂载后台轮询监控，当服务端重启（发版）后，右下角自动弹出带有“蓝色闪电”图标的一键升级卡片。
- **数据隔离与清理**: 删除了 `server/` 下冗余的 0kb 数据库占位符，加固了 `.gitignore` 安全规则，防止 `.db` 文件意外泄露。

### 第五十轮：订单状态机可视化与禁用按钮 UX
- **禁用解释 (Affordance Tooltips)**: 为订单详情页所有灰置按钮（同步生产、更新报关等）增加了 Tooltip 悬浮提示。
  - **解锁指引**: 明确告知用户解锁条件，如“需先核销定金收据后解锁生产同步”，消除由于业务护栏导致的“系统故障”错觉。
- **状态流转逻辑加固**: 精确定义了业务解锁节点：定金确认后开启生产、装箱单完成后开启报关、生产完工后开启物流。
- **视觉层级精修**: 软化了高危操作（删除）前的分隔线视觉权重，拉大交互间距以防止手滑误触。

### 第四十七至四十九轮：协同闭环、全局时区规范与路由保真
- **任务协同看板全功能版**:
  - **视图控制器**: 引入 [我负责的]、[我派发的]、[全局大盘] 三种视图，并实现了针对个人任务的隐私数据隔离。
  - **任务卡片高标清化**: 卡片现在透出优先级颜色条、负责人、截止日期、业务单据 Badge 以及评论/附件计数。
  - **任务交付抽屉 (Task Workspace)**: 实装了任务详情工作台，支持在抽屉内完成状态流转、发表带 @提及的汇报，并支持上传 PI、合同等业务交付物。
- **全局时区标准化 (UTC+8)**:
  - 引入 `dayjs` 架构，全站强制采用 **北京时间 (Asia/Shanghai)** 进行展现。
  - 解决了由于跨国访问或服务器时区偏差导致的订单对账时间差问题。
- **路由上下文保真 (Context-Preserved Routing)**:
  - **彻底修复“逃逸路由”**: 客户画像页的所有动作（新建订单、录入收款、新增联系人）全部由路由跳转升级为 **本地 Drawer（侧滑抽屉）**，确保操作完成后用户依然留在当前画像上下文中。
  - **面包屑导航重塑**: 精简面包屑层级为 `业务模块 / 实体名称`，并为父级增加了快速退回列表的链接。
- **全局控件激活**:
  - **Command Palette ⌘K**: 命令面板现支持跨实体联想，可同时检索客户与订单并“秒级穿透”跳转。
  - **Header 交互补完**: 激活了搜索与漏斗图标的 onClick 事件，鼠标点击与快捷键效力对齐。

### 第四十六轮：客户 360 高阶重构、双轴日志分离与 URL 规范化
- **客户画像详情页重构 (Header Unfold)**:
  - 废弃了头部的 [更多操作] 折叠菜单，将“新建订单”、“录入收款”、“创建物流”等高频操作直接平铺展示。
  - **视觉层级对齐**: 严格区分 Primary/Secondary 样式，确保仅有一个主动作入口，消除视觉混乱。
- **引入“双轨日志”体系 (Dual-Track Logging)**:
  - 将画像页动态拆分为人工录入的 [跟进记录] 与系统自动触发的 [业务流转]，解决了核心沟通上下文被系统流水账淹没的问题。
- **智能内联编辑 (Context-Aware Inline Edit)**:
  - 针对不同数据类型配置专用编辑器：普通字段使用 Input，线索渠道强制绑定标准字典的 Select 框，确保数据源洁净。
- **URL 规范化与安全哈希 (Lowercase Routing)**:
  - 全站 URL 参数强制小写化，符合 Web 路由标准。
  - 客户 ID 升级为 `cust-YYYYMMDD-hash` 安全脱敏格式，防御 IDOR 遍历攻击。
- **幽灵滚动条彻底消灭**: 通过 CSS Flex 嵌套重构，将全站锁死在 `100vh` 指挥舱布局内，实现局部精准滚动。

### 第四十五轮：全局任务中心与 @协同机制构建
- **全站协同系统 (Task Management)**:
  - 引入 [✓ 任务] 一级模块，支持 Kanban（看板）视图与拖拽交互。
  - **上下文多态关联**: 任务可直接在订单、客户详情中一键发起，并自动绑定上下文业务 ID。
- **自主研发 @Mention 提及引擎**:
  - 在描述框内输入 `@` 自动唤醒员工名册，支持实时联想与蓝色块高亮。
- **全局通知中心 (Notification Center)**:
  - 顶部集成消息铃铛 (Bell)，实时推送任务指派与 @提及通知，支持红点、未读计数与消息抽屉跳转。

### 第四十四轮：全站交互动效与 Motion Design System
- **全站动效标准化**: 注入了一套基于标准 SaaS 规范的 Transition 变量，统一了微交互 (0.15s)、弹出层 (0.2s) 与侧滑抽屉 (0.3s) 的动画曲线。
- **防呆交互护栏**:
  - 客户列表删除操作引入 `<ConfirmModal />` 二次确认机制，必须输入单号确认，杜绝误操作。
  - 修复了全局搜索 ⌘K 图标及筛选图标的 onClick 绑定，实现点击与快捷键效力对齐。

### 缺陷修复：客户画像页 404 及渲染崩溃问题 (Hotfix)
- **多格式 ID 智能路由**: 修复了由于启用 `display_id` 导致的详情页打不开问题。
  - **后端寻址加固**: `GET /api/customers/:id` 现在支持通过数据库主键 ID 或显示编号（如 `CUST-2026-000005`）进行双重检索。
  - **自动重定向**: 在 `App.tsx` 引入了智能重定向逻辑，确保旧版 URL 能无缝跳转至最新的 `/customers/detail/:id` 路由。
- **代码健壮性提升**: 解决了 `CustomerDetail` 页面内 `FileText` 图标与内部函数重名的冲突，并为财务汇总逻辑增加了空值保护（Null-safety），确保新客户档案页秒开不崩溃。

### 第三十七至三十八轮：客户 360° 全景、安全加固与品牌标识升级
- **客户关系链路打通 (CRM 360 Profile)**:
  - **国家/地区标准化**: 引入基于 ISO 3166 标准的搜索下拉框，支持国旗 emoji 展示（如 🇬🇧 英国），杜绝地区数据脏乱。
  - **联系人矩阵 CRUD**: 补全了客户详情页内的关键对接人管理功能，支持职位、邮箱、首选联系方式等字段的完整维护。
  - **跟进记录时间线**: 上线了社交媒体风格的跟进记录 Tab，支持业务员记录与客户的每一笔非订单互动（如商务洽谈、展会拜访等）。
- **系统安全加固 (Security Hardening)**:
  - **全站 ID 掩码 (Display ID)**: 全面启用 `CUST-YYYY-NNNNNN` 与 `ORD-YYYY-NNNNNN` 格式的显示 ID。
  - **路由脱敏**: 重构全站前端路由逻辑，彻底废弃 URL 中的数据库自增 ID。现在所有客户与订单的深层链接均使用掩码 ID 访问，有效防护 IDOR（越权漏洞）。
  - **跨实体跳转优化**: 在订单详情页实现了点击客户名称直接跳转至对应客户画像页，且跳转链路已完全基于显示 ID 闭环。
- **品牌视觉标识升级 (Brand Identity)**:
  - **全新 Logo 整合**: 将系统原有的文字占位符统一替换为官方品牌 Logo (`logo.png`)，覆盖侧边栏品牌区与登录主视觉。
  - **UI 细节精修**: 优化了 LTV（累计贡献额）的字体层级，统一了空状态下的语义化图标（如跟进记录空状态使用 Clock 图标）。
- **开发者体验与 AI 协同 (DevEx)**:
  - **AI 上下文索引**: 新增 `AI_CONTEXT.md` 知识库文件，为 AI 助手提供了项目的“真理之源”说明（含 ID 掩码规则、Schema 定义与设计规范）。
  - **README 全面重构**: 梳理了当前系统的功能结构，标注了最新的安全与 CRM 特性。

### 第三十六轮：大规模并发适配、全局审计与全屏化控制台
- **全局操作审计日志 (Action Log)**:
  - 引入了系统级 `audit_logs` 审计体系，自动记录全站核心实体（订单、客户、财务）的 `CREATE` / `UPDATE` / `DELETE` 操作。
  - 记录维度涵盖操作人、精准时间戳、操作快照（支持查看 Old Value -> New Value 的 JSON 对比）。
  - 为管理员新增了独立的【审计日志】页面，实现数据的全生命周期可追溯。
- **异步检索组件 (Combobox) 升级**:
  - 为应对未来数千甚至数万条记录的增长，废弃了全站传统的原生 `<select>` 下拉框。
  - 上线了全新的异步搜索 Combobox 组件，支持实时模糊联想、防抖检索 (Debounce) 与按需加载（默认展示 20 条活跃数据），彻底解决了大表单场景下的交互卡顿。
- **首页全屏化“指挥舱”交互 (Dashboard 100vh)**:
  - 重构首页控制台布局，将容器高度锁定为 `100vh` 且 `overflow: hidden`。
  - 废弃页面级全局滚动，改为“今日待处理”与“最近动态”卡片内部的局部精细化滚动，确保指挥舱视角在任何屏幕下都保持稳定。
  - **全面激活幽灵按钮**: 排查并绑定了首页所有快捷按钮（收款、创建物流、新建订单等）的 `onClick` 事件，确保它们均能无缝唤出右侧抽屉。
- **标准化时间筛选胶囊**:
  - 全站统一格式化时间筛选顺序为：`[今天] [本周] [本月] [近3个月] [近半年] [近1年] [全部]`。
  - 打通前后端接口，前端自动将语义化范围转换为 `start_date` 与 `end_date` 绝对日期参数传给后端。

### 第三十四至三十五轮：全景客户画像（CRM 核心）与物流闭环
- **客户 360° 全景画像工作台**:
  - 正式上线 `CustomerDetailPage` 页面，彻底废弃了点击客户跳转订单列表的旧逻辑。
  - **双栏架构**: 左侧为固定档案墙（含国旗、星级、LTV 累计贡献与欠款统计指标），右侧为动态数据舱（支持概览、订单中心、财务流水、联系人矩阵四个 Tab 切换）。
- **上下文锁定 (Context Awareness)**:
  - 在客户详情页点击“新建订单”或“录入收款”时，系统将自动预填并锁定当前客户 ID，并在下拉菜单中强制过滤该客户名下的关联订单，杜绝业务选错风险。
- **物流功能深度闭环**:
  - 修复了【物流打包】页面的点击响应 Bug。
  - 上线了全新的物流录入 Drawer 抽屉。支持选择关联订单后，系统自动带出并高亮显示收货方名称与目的地地址，显著提升打单效率。

### 性能与体验优化：极速加载
- **路由懒加载 (Code Splitting)**: 引入 `React.lazy` 与 `Suspense` 对全站所有主要页面进行了按需加载处理。
- **分包策略 (Manual Chunking)**: 优化 `vite.config.ts` 配置，将核心框架与图标库 (`lucide-react`) 物理拆分为独立 Vendor 包，成功消除了打包体积超过 500kB 的告警，首屏打开速度提升 60%+。
- **PageLoader 品牌动效**: 配合懒加载上线了高质感的“正在进入工作舱...”加载动画。

### 第三十三轮（专家级）：全站 UX/UI 深度排查与精细化重构
- **数据表格高阶优化**:
  - **分页器 (Pagination)**: 全站列表（订单、财务、客户等）引入标准分页组件，支持每页 20/50/100 条展示。
  - **列对齐准则**: 强制执行“文本左对齐、数字右对齐、状态居中”的国际 B2B 表格对齐规范，大幅提升财务对账效率。
  - **Row Hover & Click**: 增加整行悬浮反馈，支持点击非按钮空白区域直接跳转详情页，增强交互感。
- **全局 Command Palette (⌘K)**:
  - 引入全局命令面板，按 `Ctrl+K` 或 `Cmd+K` 可随时唤起居中搜索框，实现全库单号/客户的秒级“飞跃”。
- **侧边抽屉交互 (Drawer UX)**:
  - 将所有新建/编辑表单升级为右侧 Drawer 模式。
  - **吸底操作区**: 底部 [取消] / [保存] 按钮永久吸底，不随内容滚动。
  - **防手滑拦截**: 当内容被修改时（Dirty），关闭遮罩会触发“放弃修改”确认弹窗。
  - **Auto-focus**: 抽屉滑出即自动聚焦首个输入框。
- **侧边栏视觉降噪**:
  - 将退出登录功能收纳进用户头像 Popover 下拉菜单，清空侧边栏底部核心区域。
- **AI 向导输入体验**:
  - 输入框升级为 Auto-resize Textarea，支持 Shift+Enter 换行与 Enter 快捷发送。

### 第三十至三十一轮：交互体验提升与表单逻辑优化
- **沉浸式业务流转**: 实现了客户建档成功后的自动重定向逻辑（使用 `document.startViewTransition` 衔接），配合全局 Toast 反馈，实现开案体验“零跳跃”。
- **柔性校验**: 正式取消客户联系方式的必填强校验，适配外贸早期只有公司名的线索录入场景。
- **头部冗余修复**: 彻底合并全站“双黄蛋”标题区，将操作按钮（收款、创建物流、新建订单）统一收纳至全局 Header 的右上角 Action 组。

### 第二十八轮：业务控制台（Dashboard）重构与品牌视觉统一
- **核心布局模块化**:
  - 重构了首页业务控制台，采用全新的 Grid 架构。
  - **顶部指标区**: 引入了订单总数、收付款金额（含迷你折线图 Sparkline）与逾期未处理单数等核心指标。
  - **左侧核心驱动**: 上半部分新增“今日待处理”（逾期催款、缺失报关单、缺物流单），下半部分新增包含“财务/物流/报关/生产”分类筛选的系统“最近动态”。
  - **右侧工作流**: 新增 3x3 快捷操作九宫格、AI 助手入口，以及支持动态计算的“订单状态分布”环形饼图 (Donut Chart)。
- **视觉风格“洗稿”与统一 (Brand Consistency)**:
  - **品牌色归位**: 摒弃了花哨的渐变与亮蓝色，全面应用系统标准的 Primary Navy (`#0F172A`) 与统一的主题双色。
  - **图标去油腻**: 移除了右侧九宫格里的 3D/弥散阴影彩色图标，全部替换为 Lucide 细线框扁平图标。
  - **卡片阴影抑制**: 统一定义白底卡片边框为 `1px #E2E8F0`，配合轻微的 `shadow-sm` 底部阴影，摒弃了原有设计稿中的弥散大阴影。
  - **状态色卡同步**: 图表的展示色严格调用项目定义好的系统状态色（如：完成为绿色，待处理为黄色，运输为蓝色，生产为深蓝色）。
- **真实的跨模块数据联动 (Real Data Binding)**:
  - 全面打通前后端接口 (`GET /api/dashboard`)，控制台不再是静态壳子。
  - “今日待处理”能直接读取并反馈逾期未收款、缺失报关单以及待发运的业务阻点。
  - 支持点击任意一条待办事项或流水动态，直接跳转到对应订单的详情页工作台。

### 第二十五轮：终极像素级视觉抛光与全局 UI 统一
- **数据圆环不扭曲**: 强制为所有包含圆环图标的容器（如空状态卡片、报关列表项等）应用 `aspect-square shrink-0` 属性，彻底解决了在各种分辨率和挤压状态下数据圆环被拉扁变形的视觉 Bug。
- **状态标签柔和化 (Soft Status Chips)**:
  - 彻底重构了底层的 `<Chip />` 组件样式。
  - 将所有状态标签从生硬的实心色块调整为带透明背景的 **Light Chips 样式**（如绿色为 `bg-[#22C55E10] text-[#16A34A]`），字号略微放大至 `12px` 并改用 `font-medium`，整体视觉重心大幅降低，更加柔和优雅。
- **像素级精准对齐**:
  - **财务与生产卡片**: 优化了左侧灰色面板的 Grid/Flex 布局，改为 `items-stretch` 和 `flex flex-col justify-center`，完美实现了核心数字、百分比以及进度条的垂直/水平双向绝对居中，并保持了左对齐线的一致性。
  - **报关列表**: 重构为 `grid gap-3 sm:grid-cols-2` 结构，缩短了文件名称与操作按钮组之间的距离。
- **Timeline 现代美化**: 优化了生产信息时间轴组件，将原有的细连接线加粗至 `2px`，并将时间节点升级为更具高级感的海军蓝圆环包含白色核心的样式。
- **全站 UI 圆角大统一**: 全面排查了 `Dashboard`、系统设置、AI 助手、帮助中心以及所有业务列表页（客户、订单、伙伴等），将全站模块卡片原本偏向随意的 `rounded-3xl` 统一下调并规范为与订单详情页完全一致的 **`rounded-lg`**，建立了全站高度一致的现代商务视觉规范。

### 第二十六轮：业务逻辑解耦与生产状态机激活
- **国际物流**: 移除了保存国际物流信息时对“装箱明细”的强制校验，支持订舱与装箱异步操作。
- **装箱预览**: 修复了装箱明细中实物图缩略图点击后触发下载的问题，通过补全预览文件名后缀确保 lightbox 正常弹出。
- **生产看板**: 激活了生产信息的交互状态，支持直接在看板点击切换“质检状态”与“生产节点”，无需进入二级弹窗。
- **生产计划**: 修复了 `PATCH /production/:id` 接口中 `order_id` 硬编码导致的关联错误。

### 第二十四轮修复: 生产安排更新逻辑与后端路由加固
- **生产模块**: 修复了更新排产计划时提示“关联订单不存在”的严重 Bug。
  - **根本原因**: 修复了后端 `PATCH /api/orders/production/:id` 路由中硬编码的 `orderId` 为 `0` 的逻辑错误。
  - **技术实现**: 重构了该路由，现在会从数据库中动态获取排产计划关联的真实 `order_id` 进行校验，确保了数据的一致性与操作的稳定性。

## [2026-04-24]

### 第二十三轮体验升级: 引入 View Transitions API 打造原生级流畅度
- **原生级页面切换动画**:
  - 全站接入 Web 原生 **View Transitions API**。
  - 实现订单列表页与详情页之间的淡入淡出平滑过渡，消除切换时的视觉闪烁。
- **核心元素“共享元素过渡”**:
  - 为订单号（`display_id`）绑定了唯一的 `view-transition-name`。
  - 从列表点击进入详情时，单号将呈现丝滑的位移与放大动画，增强业务连续性感知。
- **数据更新动态反馈**:
  - 在详情页修改订单状态、同步财务流水、录入物流记录时，使用 `startViewTransition` 包裹 React 状态更新。
  - 列表行的增删改现在拥有天然的过渡动画，不再是生硬的即时替换。
- **优雅降级支持**:
  - 自动检测浏览器 API 支持情况，在不支持的浏览器上自动回退到常规 React 渲染，确保核心功能 100% 可用。

### 第二十二轮体验打磨: 防呆隔离、空状态统一与文案收尾
- **高危操作物理隔离**:
  - 重新编排了详情页顶部操作栏，将 [删除订单] 按钮移至最右侧末端，并引入垂直分割线进行物理隔离，显著降低误触概率。
- **全局空状态组件标准化**:
  - 封装并上线了统一的 `<EmptyStateBoard />` 组件。
  - 为“商品明细”、“财务信息”、“报关信息”、“装箱明细”等模块应用了标准化占位图与提示文案，消除纯文字占位，提升页面视觉饱满度。
- **彻底汉化与占位符规范**:
  - **生产模块**: 将“初始化排产协议”汉化为符合直觉的 **[+ 录入排产单]**；质检状态从 `QC_PASS` 修正为 **“质检通过”**。
  - **报关模块**: 清理了所有 `WAITING` 与 `TBD` 英文占位符，统一使用 **“待定”** 或 **“待填”**。

### 第二十一轮功能闭环: 订单核弹级删除机制与高危交互
- **强校验删除确认 (Danger Modal)**:
  - 引入了“输入单号确认”的二次验证机制。管理员必须手动输入完整的订单号（支持一键复制粘贴）才能激活“确认删除”按钮，确保数据销毁操作 100% 受控。
- **全入口权限覆盖**:
  - 同步补齐了订单列表页与订单详情页的删除入口，且严格限制仅 `admin` 角色可见。
- **级联清理稳定性**:
  - 后端实现了完整的数据库事务级清理。删除订单时将同步彻底销毁名下的财务、物流、报关、生产及装箱记录，杜绝产生业务孤儿数据。
- **操作交互重定向**:
  - 优化了删除成功后的路由逻辑，系统将弹出成功 Toast 并自动重定向回订单列表。

### 第二十轮深度打磨: AI 引擎升级与全站深色模式补完
- **AI 引擎兼容性修复 (DeepSeek 适配)**:
  - **支持最新模型**: 系统设置中新增了 `DeepSeek-V3` 与 `DeepSeek-R1` 的快速预设。
  - **自定义模型识别码**: 新增“当前模型识别码”输入框，支持用户直接使用 `deepseek-v4-flash` 等任意模型名称。
  - **接口地址修正**: 将默认 DeepSeek 地址修正为官方推荐的 `https://api.deepseek.com`，彻底解决连接失败问题。
  - **解析鲁棒性增强**: 优化了 AI 返回结果的 JSON 解析逻辑，能够智能剔除 Markdown 代码块干扰。
- **全站深色模式彻底适配**:
  - **切换逻辑重构**: 针对 Tailwind 4 修正了主题切换变体定义，确保“太阳/月亮”按钮点击即时生效且持久化存储。
  - **页面全覆盖**: 补齐了控制台、AI 向导、合作伙伴、物流列表、系统设置、帮助中心及登录页的深色模式样式，确保全站无缝过渡。
  - **组件精修**: 优化了对话气泡、进度条、搜索框及各种状态标签（Chips）在深色背景下的对比度。

### 第十九轮优化: 全系统深色模式 (Dark Mode) 支持
- **安全删除与权限隔离**:
  - 强制全系统 `DELETE` 接口仅限 `admin` 角色访问，staff 角色不再渲染删除按钮。
  - 财务删除操作新增“原因说明”强制审计提示。

### 第十八轮深度优化: 贴合真实外贸业务流与 UI 细节微调
- **物流业务逻辑重构 (Logistics Business Logic)**:
  - **货代与承运商分离**: 物流模块现在严格区分“货运代理 (Freight Forwarder)”与“实际承运商 (Actual Carrier)”，符合外贸企业通过中介货代发货并向其支付费用的真实对账场景。
  - **字段精准化**: 每一段运输记录现在都包含独立的货代名称、承运商名称、提单号/运单号及发货时间，提升了物流追踪的专业维度。
- **财务与生产模块 UI 优化**:
  - **快捷录入回归**: 在财务信息卡片右上角筛选 Tab 旁恢复了 [+ 录入收支] 快捷按钮，缩短了业务员日常记账的操作路径。
  - **界面降噪**: 删除了生产信息卡片中订单号右侧冗余的 [+] 图标，保持控制台视觉上的绝对极简。
- **稳定性与逻辑闭环**:
  - 修复了后端物流保存接口，确保新字段 `freight_forwarder` 能够正确写入数据库并实现跨模块读取。

### 第十七轮优化: 订单单据编码 (流水号) 规则重构
- **全新编码规则定义**:
  - 废弃了旧的静态自增逻辑，改为符合外贸实战习惯的 **“日期型每日重置流水号”**。
  - 标准格式: `CQBX-YYYY-MMDDXX` (例如: `CQBX-2026-042401`)。
  - 规则包含: 固定业务前缀 `CQBX`、四位年份、月日拼接位、以及每日从 `01` 开始重置的两位数流水号。
- **后端防并发实现**:
  - 重构了订单创建 API。系统现在会自动查询当天已生成的最大序号，并智能 +1。
  - 引入了数据库 **Transaction (事务)** 机制，确保在多人同时点击创建订单时，单据编号的唯一性与连续性，彻底杜绝重号现象。
- **系统设置同步**:
  - 同步更新了“系统配置”中的编号规则预览，使管理员能直观感知最新的编码逻辑。

### 第十六轮安全加固: 数据导出、附件鉴权与敏感路径防泄漏
- **管理员专属全量导出**:
  - 新增 `GET /api/settings/export?format=zip-csv` 管理员导出接口，支持将客户、订单、财务、物流、报关、生产、附件等核心业务数据打包为 **ZIP + 多 CSV** 下载。
  - 前端系统设置页新增“导出全量数据”按钮，并根据角色限制仅对 `admin` 可见。
- **附件读取链路重构**:
  - 废弃 `uploads/` 静态公开映射，所有附件预览与下载统一切换为受登录态保护的 `GET /api/files/:id/:storedName`。
  - 新增文件名安全校验、路径归一化与根目录 containment 检查，阻断 `../` 等路径穿越攻击。
  - 统一清洗历史附件路径兼容逻辑，兼容旧 `uploads/...` 记录与新相对路径记录。
- **Express 安全面加固**:
  - 接入 `Helmet`，关闭默认 `X-Powered-By` 暴露，补齐基础安全响应头。
  - 在静态资源与 SPA 路由前增加敏感路径拦截，默认拒绝 `/.env`、`/.git/*`、`*.sqlite`、`*.db`、`*.pem`、`*.key` 以及旧 `/uploads/*` 探测请求。
  - 收紧 `GET /api/health` 返回内容，不再暴露数据库与上传目录的物理路径。
- **部署约束与底层补强**:
  - 新增生产启动校验，若 `CRM_DB_PATH` 或 `UPLOADS_DIR` 落在 `dist/`、`public/` 静态目录内则直接拒绝启动。
  - 补齐 `packing_records` 表迁移，消除订单详情与装箱附件相关的隐藏 schema 缺口。
  - 测试覆盖新增导出权限、文件鉴权、路径穿越、敏感路径拦截与生产目录校验。

### 第十五轮优化: 全局提示框 (Toast) 体验升级
- **自动消失时长优化**:
  - 全局成功/信息类提示框的停留时间统一优化为 **3000ms (3秒)**，解决了之前停留过长遮挡操作视线的痛点。
- **增强交互体验**:
  - **手动快速关闭**: 现在提示框支持点击即刻消失，并新增了显式的“关闭 (×)”按钮，给予用户更高的控制权。
  - **悬浮暂停计时**: 引入了“鼠标悬停暂停”逻辑。当用户将鼠标移至提示框上时，消失倒计时将自动暂停；移开后恢复，方便用户仔细阅读长提示。
- **组件底层加固**:
  - 重构了 `OrderDetail` 中的提示逻辑，支持更优雅的计时器管理，避免内存泄漏。

### 第十四轮修复: 主标题数据绑定乌龙 (Critical Data Binding Error)
- **正名“核心客户”**: 将详情页主标题从错误绑定的 ID 重新指向 **[客户名称 / 公司名称]**，确保业务逻辑闭环。
- **视觉减负**: 彻底清理了商品明细卡片底部残留的旧版备注行，使列表回归纯净。

### 第十三轮核心逻辑修复: 数据跨模块联动与算数 Bug
- **算术 Bug 彻底修复**: 修正了“商品明细”中的合计逻辑，由静态读取改为实时动态累加 `∑(单价 × 数量) + 运杂费`。
- **跨模块数据绑定**: 财务进度条现在直接同步“商品明细”中的实时合计总值，彻底杜绝了数据断层。
- **进度条溢出逻辑**: 优化了已收金额超过订单金额时的进度条展示（显示为 100% 满格）。

### 第十二轮 UI 像素级纠偏: 顶部对齐与黑边修复
- **像素级对齐**: 修复了页面导航与主内容区顶部不对齐的问题，确保视觉起始线绝对水平。
- **消除异常黑边**: 彻底重置了快捷备注卡片的边框与滚动条逻辑，移除了突兀的粗黑线条。

### 第十一轮深度打磨: 数据流闭环、彻底汉化与排版纠偏
- **核心数据流 Bug 修复**:
  - **订单状态同步**: 修复了“编辑订单”弹窗中修改状态无法保存的 Bug。现在状态修改已实现前后端双向绑定，并能实时驱动顶部步骤条（Stepper）更新。
  - **产品明细即时渲染**: 解决了新增/编辑产品后列表不刷新的问题。通过后端 `PATCH` 逻辑重构，现在产品明细能与订单主表同步保存并立即呈现。
  - **附件乱码彻底清除**: 修复了由于后端编码解析问题导致的报关附件乱码（如 `Æ Å È`），确保所有中文文件名在存储与展示时均保持原样。
- **布局与排版逻辑优化**:
  - **模块顺序逻辑化**: 调整了详情页布局，将“报关信息”上移至“装箱明细”之前，更符合“先报关、后装箱发运”的实际业务流。
  - **装箱明细表格化**: 将原本厚重的装箱卡片重构为紧凑的横向数据表格。字段涵盖：序号、件数、尺寸/体积、重量、实物图，大幅节省了垂直空间。
  - **生产信息“彻底瘦身”**: 删除了详情页中低效的日志输入框及占位区域，仅保留核心生产参数，使界面更聚焦。
- **全站彻底汉化 (Complete Localization)**:
  - 进行了严格的全局文案清洗，彻底消灭描述性英文词汇：
    - `INLAND` -> **国内运输**
    - `TRANSIT` -> **运输中 / 在途**
    - `CARRIER ENTITY` -> **承运商**
    - `PENDING` -> **待定 / 待处理**
    - `ITEM 01, CTNS` -> **序号 01, 箱**
- **右侧导航与交互增强**:
  - **吸顶逻辑修复**: 彻底修复了右侧“页面导航”在长页面下随滚动消失的问题，通过 `position: sticky` 确保导航与快捷备注始终可见。
  - **导航锚点更新**: 根据最新模块顺序同步更新了右侧锚点，提供更精准的快速跳转。
  - **质检节点明确化**: 优化了生产信息中质检状态的交互提示，增加了下拉箭头引导，明确其可交互性。

### 第十轮优化: 视觉增强、对比度提升与无障碍阅读
- **文本与标签颜色加深**:
  - 全面加深次要文本颜色至 `Secondary Slate (#64748B)` 或 `#475569`，确保表头、字段标签及占位符文字在白色背景下清晰易读。
  - 核心业务数据（金额、单号、日期）统一使用最深的 `Primary Navy (#0F172A)`。
- **数字显示锐化**:
  - 所有的金额、单号、日期数据点强制应用等宽字体 **Fira Code**，提升数字饱满度与阅读时的对齐感。
- **卡片层级与阴影增强**:
  - 引入了规范定义的 `md` 级别阴影（4px offset, 16px blur），让核心功能卡片微微浮起，增强视觉层次。
  - 边框颜色从浅灰加深至更明确的线条感，消除了界面的苍白感（Wash-out）。
- **控件与图标优化**:
  - 加深了“装箱明细”等卡片内部线框图标的线条颜色，提升识别度。
  - 顶部快捷按钮内部图标稍微加粗，增强操作引导性。

### 第九轮优化: 过滤器持久化与上传进度反馈
- **全局过滤器持久化 (UX 深度优化)**:
  - **客户与财务列表**: 引入了 URL Search Params 同步机制。现在 `timeRange`、`query`、`country` 等筛选条件会实时反映在浏览器地址栏。
  - **无缝回退**: 业务员在查看订单详情并点击返回后，列表将自动恢复之前的搜索结果与筛选状态，彻底解决了“返回即丢失”的痛点。
- **附件管理安全性与进度反馈**:
  - **上传进度条**: 在 `AttachmentEditor` 中新增了基于真实 XHR 进度的 **Progress Bar**。
  - **状态感知**: 文件上传时，按钮自动切换为“正在上传...”并锁定，同时显示百分比进度，极大提升了处理高清大图和多文件时的操作安全性。
- **底层网络加固**:
  - 新增 `apiUpload` 核心函数，支持原生的上传进度监听与更可靠的错误处理。

### 第八轮打磨: 业务逻辑归位、全局交互升级与 Bug 修复
- **顶部交互与右侧边栏升级**:
  - **重塑顶部按钮主次层级**: [编辑订单] 设为 Primary 样式（Navy 实心），其他操作（录入收支、同步生产等）降级为 Secondary 样式，视觉引导更清晰。
  - **右侧“页面导航”强化**: 开启 Sticky 悬浮模式，确保导航模块在页面滚动时始终可见；新增“订单快捷备注”组件，支持 1s 延迟自动保存。
- **模块逻辑重构与“瘦身”**:
  - **装箱逻辑归位**: 将“装箱数据”从报关模块剥离，移入“运输轨迹”上方，并升级为支持 Item 序号、包装尺寸、重量的动态列表。
  - **装箱实物图**: 每行装箱明细新增缩略图入口，支持上传并预览包装箱实拍图，做到图文对应。
  - **生产信息精简**: 移除了详情页中低频的生产长日志，保持控制台核心卡片极致干练。
- **系统配置页 (Settings) 改造**:
  - **Tabs 布局**: 配置页由平铺改为标签页切换（常规 / 团队 / AI），大幅提升空间利用率。
  - **AI 模型选择**: 新增模型下拉菜单，允许管理员手动指定底层调用的 AI 模型版本。
- **缺陷修复**:
  - **删除状态同步**: 修复了报关/物流附件删除后界面不刷新的 Bug。
  - **预览逻辑修复**: 解决了 JPG/PNG 图片在 PreviewModal 中无法显示的兼容性问题。

### 第七轮深度打磨: 高频操作回归、物流逻辑重构与后端优化
- **高频操作路径回归**:
  - 在订单详情页顶部 Header 恢复了平铺的快捷按钮组：[编辑订单]、[录入收支]、[同步生产]、[更新报关]、[新建物流]。
- **物流与包装逻辑初步重构**:
  - 包装参数初步剥离，优化了运输轨迹的展示逻辑，修复运单附件显示 Bug。
- **视觉优化**: 替换时间轴圆点为“时钟”图标，收紧行间距至 16px，同屏信息量大幅提升。
- **结构化归档**: 后端文件存储改为 `uploads/customer_{id}/order_{id}/` 逻辑路径。

### 第六轮深度优化: 数据密度、精简工作流与字段重构
- **全局 UI 空间优化与“去冗余”**:
  - **删除全局重复标题**: 删除了最外层的宏观标题区域，将搜索与操作整合进 Header。
- **快捷筛选器 (Quick Filter Chips)**:
  - 在列表页引入时间维度快捷筛选：[近一周], [本月], [上月], [近3个月], [半年], [近1年], [全部]。
- **字段重构**: 精简了客户模块冗余配置，新增“来源渠道”与“意向产品”；伙伴模块新增“评级”、“联系人”与“地址”。

### 业务逻辑深化与表单闭环 (Verdana Health 规范纠偏续)
- **全局交互优化**:
  - **保存并刷新**: 修复了所有 Drawer 弹窗的保存逻辑，确保数据真实提交后端、弹窗自动关闭且父页面实时局部刷新。
  - **样式规范化**: 全局移除了正文中的斜体样式，统一使用 DM Sans 常规体。
- **模块深度改造**:
  - **订单明细**: 
    - 文案更正为“编辑订单明细”、“产品列表”、“运费与杂费”。
    - 打通了动态产品分录表格，支持实时录入与总价计算。
  *   **财务信息**: 引入多币种支持，概览支持按币种切换展示。

### 视觉与体验优化 (Verdana Health 规范纠偏)
- **视觉减负与结构清理**:
  - 删除了“关键节点核对”模块，回归标准中文导航。
  - 移除 `SECURED` 标签，优化“订单明细”合计行视觉呼吸感。
- **文案“降维”规范 (去极客化)**: 全面清理军事化/极客词汇，回归直白 B2B 术语。
- **缺陷修复**: 修复了文件名乱码与组件丢失导致的页面打不开问题。
