# Ensure native optional dependencies are installed for the target platform.
# Without this, cross-platform deployments (e.g. macOS → Linux server) may
# fail to load Vite's rolldown native binding.
optional=true
